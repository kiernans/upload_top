<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title><%= title%></title>
</head>

<body>
    <%- include('navbar', {files: true}) %>
    <h1><%= title%></h1>
    <div>
        <% if (content) {%>
            <%content.forEach((item) => { %>
                <a href=<%= item.link%>><%=item.name%></a>
            <%})}%>
    </div>
</body>

</html>